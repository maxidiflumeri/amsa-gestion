// Re-export notistack SnackbarProvider con configuración por defecto de AMSA Gestión.
// Usado internamente por AppProviders — no necesita usarse directamente en las páginas.
export { SnackbarProvider } from 'notistack';
