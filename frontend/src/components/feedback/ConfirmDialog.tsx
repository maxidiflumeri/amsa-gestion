// Re-export del ConfirmProvider y useConfirm — el diálogo está integrado en el provider.
export { ConfirmProvider, useConfirm } from '../../context/ConfirmContext';
export type { ConfirmOptions } from '../../context/ConfirmContext';
