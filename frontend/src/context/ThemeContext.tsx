// Re-export de compatibilidad: mantener este archivo para no romper imports legacy
export { ColorModeContext as ThemeContext, ColorModeProvider as ThemeContextProvider, useColorMode } from './ColorModeContext';
